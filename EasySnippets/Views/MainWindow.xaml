<Window x:Class="EasySnippets.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySnippets"
        x:Name="EasySnippetsWindow"
        mc:Ignorable="d"
        Title="ES" Height="300" Width="400" Closing="Window_Closing" WindowStyle="ToolWindow">

    <Grid>
        <Grid.DataContext>
            <Binding ElementName="EasySnippetsWindow" Path="." />
        </Grid.DataContext>

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="Open..." Click="MenuOpen_Click" />
                <MenuItem Header="Save As..." Click="MenuSaveAs_Click" />
                <MenuItem Header="_Exit" Click="Exit_Click" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Always on top" IsCheckable="True" IsChecked="False" Checked="AlwaysOnTopToggle"
                          Unchecked="AlwaysOnTopToggle" />
            </MenuItem>
            <MenuItem Header="+ Add new" Click="AddNewClick" />
        </Menu>

        <DataGrid ItemsSource="{Binding SnippetsList}"
                  AutoGenerateColumns="False"
                  Name="SnippetsDataGrid"
                  SelectionChanged="SnippetsDataGrid_OnSelectionChanged"
                  MouseDoubleClick="SnippetsDataGrid_OnMouseDoubleClick"
                  Margin="0,20,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay}" Width="*" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>

</Window>